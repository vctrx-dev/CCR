@AGENTS.md

Before creating shared behavior, follow the **Reusable Boundary Map** in `AGENTS.md`. Reuse or
extend its named boundary with regression coverage instead of adding a feature-local duplicate.

Before creating shared behavior, follow the **Reusable Boundary Map** in `AGENTS.md`. Reuse or
extend its named boundary with regression coverage instead of adding a feature-local duplicate.
